import React from 'react';

const textStyle = {
    fontSize: '20px',
    textAlign: 'center',
}

const BannerText = () => {
    return(
        <div style={textStyle}>
            Jin's Image Search Project
        </div>
    );
}

export default BannerText;